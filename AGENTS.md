# Codex Notes

- After every project change, publish to GitHub with `bash deploy.sh "<commit message>"`.
- Always report the generated version number and GitHub Pages URL to the user after publishing.
